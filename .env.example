# Base URL of the sync server as seen from the browser. Leave unset in
# development: Vite proxies /api and /ws to http://localhost:8787.
# Production (set in the Vercel project): VITE_SYNC_URL=https://whiteboard-sync.fly.dev
VITE_SYNC_URL=
